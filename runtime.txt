import os
from app import app, socketio

if __name__ == '__main__':
    # Uvicorn ASGI entrypoint
    import uvicorn
    uvicorn.run(socketio, host='0.0.0.0', port=int(os.getenv('PORT', 5000)))
